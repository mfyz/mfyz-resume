{
  "name": "mfyz-resume",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "node packages/cli/cli.js",
    "generate": "npm run build -w packages/generator"
  }
}
