{
  "name": "@mfyz/resume-generator",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "validate": "yamlresume validate ../../resume.yaml",
    "build": "yamlresume build ../../resume.yaml -o output/ && node postbuild.js && cd output && tectonic resume.tex",
    "dev": "yamlresume dev ../../resume.yaml -o output/"
  },
  "dependencies": {
    "yamlresume": "latest"
  }
}
